# Kong gateway - this is the main entry point
kong:
  enabled: true
  proxy:
    type: LoadBalancer
    loadBalancerIP: 10.1.0.110
    annotations:
      metallb.universe.tf/address-pool: k8s-services-pool
      external-dns.alpha.kubernetes.io/hostname: k8s-dashboard.dgreaves.com.
    http:
      enabled: false
